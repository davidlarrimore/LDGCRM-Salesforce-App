<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Agency_Acronym__c</fullName>
    <description>Displays Level 1 Agency Acronym</description>
    <externalId>false</externalId>
    <formula>IF(NOT(ISBLANK(Parent.Parent.Parent.Parent.Id)), Parent.Parent.Parent.Parent.Account_Acronym__c,
IF(NOT(ISBLANK(Parent.Parent.Parent.Id)), Parent.Parent.Parent.Account_Acronym__c,
IF(NOT(ISBLANK(Parent.Parent.Id)), Parent.Parent.Account_Acronym__c,
IF(NOT(ISBLANK(Parent.Id)), Parent.Account_Acronym__c,
IF(ISBLANK(Parent.Id), Account_Acronym__c,
&quot;&quot;)))))</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Agency Acronym</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
